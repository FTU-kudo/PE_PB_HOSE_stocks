# ── vnstock API Key ────────────────────────────────────────────────────────────
# Đăng ký tại: https://vnstocks.com/insiders-program#tiers
# Nếu không có key: pipeline vẫn chạy ở chế độ Guest (20 req/phút, ~24 phút/lần)
# Nếu có key: tốc độ tăng 3-5x (60-100 req/phút, ~5-8 phút/lần)
VNSTOCK_API_KEY=your_api_key_here
